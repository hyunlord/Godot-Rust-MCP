{
  "mcpServers": {
    "godot-rust-harness": {
      "command": "uv",
      "args": ["run", "--with", "mcp[cli]", "--with", "websockets", "python", "-m", "src.server"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "PROJECT_ROOT": "."
      }
    }
  }
}
